// Brute force


// Moore vooting algo

#include <bits/stdc++.h>
using namespace std;
int Majority(int arr[], int n)
{
    int count = 1, element[2];
    for(int i=0;i<n;i++)
    {
        cin
    }
    return 0;
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    return 0;
}